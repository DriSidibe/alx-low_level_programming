#include "main.h"
#include <stdlib.h>
#include <ctype.h>

/**
 *_abs - computes the absolute value of an integer.
 *@n: the parameter
 *
 *Return: check the code
 */
int _abs(int n)
{
	return (abs(n));
}
